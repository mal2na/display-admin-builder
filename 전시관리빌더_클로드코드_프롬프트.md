# 전시/관리 기능 빌더 — Claude Code 프롬프트 세트

정책서(`NC_전시관리기능_정책서_간소화_v0_31`, POL-DSP v0.31)와 참고 화면
(`https://main-concept1.vercel.app/`)을 기준으로 작성했습니다.

## 사용 방법

1. 새 프로젝트 폴더를 만들고 그 안에 아래 두 파일을 넣어두세요.
   - 정책서 원본 (`NC_전시관리기능_정책서_간소화_v0_31.html`)
   - 이 프롬프트 파일 (`전시관리빌더_클로드코드_프롬프트.md`)
   - 가능하면 `main-concept1.vercel.app` 화면 스크린샷 1~2장도 `reference/` 폴더에 같이 넣으세요.
     (클로드 코드는 브라우저를 직접 못 열 수 있으니, 스크린샷을 눈으로 보여주는 게 훨씬 정확해요.)
2. 터미널에서 그 폴더로 이동해 `claude`를 실행하세요.
3. 아래 **Phase 0 → 1 → 2 → 3 → 4** 프롬프트를 **한 번에 다 붙여넣지 말고, 순서대로 하나씩** 실행하세요.
   각 Phase가 끝나면 결과를 직접 눈으로 확인하고 다음 Phase로 넘어가는 걸 추천해요.
4. Phase 4까지 끝나면 "블록 조립 → 메인화면/이벤트화면 출력"이 실제로 동작하는 MVP가 나옵니다.
   Phase 5(배너 캠페인 통합관리 / AI 후보 / A/B 실험 / 성과 리포트)는 정책서에 있지만
   운영 복잡도가 높은 영역이라 MVP 이후 확장 단계로 분리했어요.

---

## Phase 0 — 프로젝트 컨텍스트 심기 (CLAUDE.md 생성)

```
이 프로젝트 루트에 CLAUDE.md 파일을 만들어줘. 아래 내용을 반영해서 정리해줘.

[프로젝트 목적]
개발자 없이도 운영자가 "블록 조립"하듯 화면을 구성할 수 있는 전시/관리 어드민 빌더를 만든다.
Atom → Component → Corner → Template → Container 5단 계층 구조로 화면을 표준화하고,
운영자가 화면을 새로 코딩하지 않고 표준 구성 단위를 조합해 메인화면·이벤트화면 등을
만들고, 검수 → 승인 → 게시까지 처리할 수 있어야 한다.

[핵심 개념 - 반드시 이 정의를 그대로 따를 것]
- Atom: 문구, 이미지, CTA 버튼, 가격, 아이콘, 배지, 대체텍스트처럼 화면을 이루는
  가장 작은 표시 요소. 단독으로 화면을 구성하지 않고 Component의 재료로만 쓰인다.
- Component: Atom을 조합한 기능 모듈(배너, 상품 카드, 탭 메뉴 등).
  반드시 정보형/행동형/혜택형/선택형/배너형/상품형 중 하나의 유형(type)을 가진다.
- Corner: Component를 올려놓는 화면 영역. Corner 유형(상품형/배너형/혜택오퍼형/
  업무진입형/상태안내형/콘텐츠안내형/개인화추천형/고정필수노출형)에 따라
  붙일 수 있는 Component 유형이 제한된다. 정렬 기준, 최대 노출 개수를 가진다.
- Template: 여러 Corner를 순서/배치로 구성한 화면 레이아웃. 로그인 여부, 고객
  세그먼트 등 조건 그룹에 따라 같은 Container라도 다른 Template으로 분기될 수 있다.
  기간을 가진 Template(기간형 Template)으로 특정 기간만 다른 화면을 보여줄 수 있다.
- Container: 메인화면, 메뉴, 기획전, 혜택 영역처럼 채널에 실제로 나가는 화면/영역
  단위. 화면과는 1:1, Template과는 1:N 관계. Container는 삭제하지 않고 상태값으로
  관리한다(비활성화 등). 기본 Template을 반드시 1개 가진다.

[전시 상태 흐름 - 10단계, 반드시 이 상태값을 그대로 사용할 것]
초안 작성중 → 검수 대기 → (반려 시) 수정 필요 → 검수 대기 → 승인 완료 →
예약 대기 → 게시 중 → 종료
예외 흐름: 게시 중 → 게시 중지 → 롤백 완료 / 게시 중 → 개인화 제한 → 게시 중 복귀

[참고 디자인]
reference/ 폴더의 스크린샷은 이 빌더로 최종적으로 만들어내야 하는 "결과 화면"의
예시다 (SKT 계열 앱 홈 화면: 디바이스 프레임 선택, 혜택/VIP Pick/추천 카드
섹션들이 세로로 쌓인 구조). 빌더 자체의 UI 디자인은 이 화면을 따를 필요 없지만,
빌더가 최종적으로 "만들어낼 수 있어야 하는" 화면의 복잡도 기준으로 참고할 것.

[정책서 원본]
NC_전시관리기능_정책서_간소화_v0_31.html 이 같은 폴더에 있다. 데이터 모델이나
정책 판정 기준이 애매하면 이 파일에서 해당 정책 ID(PG-DSP-xxx, PI-DSP-xxx)를
찾아서 확인하고 반영해라. 마음대로 새 규칙을 만들지 말 것.

[기술 스택 - 제안, 필요하면 조정 가능하되 이유를 먼저 말할 것]
- Next.js (App Router) + TypeScript
- 상태/스토리지: 처음엔 로컬 SQLite(예: better-sqlite3 또는 Prisma+SQLite)로
  시작. 나중에 실제 DB로 교체 가능하게 스키마를 분리해서 설계.
- 빌더 UI의 드래그앤드롭: dnd-kit
- UI: Tailwind + shadcn/ui

지금은 코드를 작성하지 말고, CLAUDE.md 작성과 함께 위 내용을 네가 이해한 대로
요약해서 나에게 다시 설명해줘. 특히 5단 계층 구조와 상태 흐름을 잘못 이해한
부분이 없는지 내가 확인할 수 있게 해줘.
```

> 이 단계의 목적은 "코드 생성"이 아니라 **클로드 코드가 개념을 정확히 이해했는지 확인**하는 것입니다.
> 설명이 틀리면 다음 Phase로 넘어가지 말고 바로잡아주세요.

---

## Phase 1 — 데이터 모델 설계 (스키마만, 코드는 최소한으로)

```
Phase 0에서 정리한 개념을 기준으로 데이터 모델(스키마)을 설계해줘.

요구사항:
1. Atom, Component, Corner, Template, Container 5개 엔티티와 그 관계를 정의해줘.
   - Corner는 corner_type을 가지고, corner_type별로 허용되는 component_type
     목록을 별도 매핑 테이블(혹은 상수)로 관리해줘. (정책서 PI-DSP-CMP-003 참고:
     "Corner 유형을 선택하면 연결 가능한 Component 유형을 자동으로 제한한다")
   - Template은 Container에 속하고, condition_group(예: "비로그인", "로그인",
     "조건충족", "조건미충족" 등 운영자가 정의 가능한 값), is_default(기본
     Template 여부), 기간(start_at, end_at, nullable) 필드를 가져야 해.
   - Container는 default_template_id를 반드시 가진다. Container는 soft-delete만
     허용 (status: active/inactive), 물리 삭제는 없음.
2. 정책서의 10단계 상태값(초안 작성중/검수 대기/수정 필요/승인 완료/예약 대기/
   게시 중/게시 중지/종료/롤백 완료/개인화 제한)을 상태 enum으로 만들고, 어떤
   엔티티(Template? Container-Template 매핑? Component?)에 붙는 상태인지
   너가 정책서를 다시 확인해서 판단해줘. 애매하면 나한테 먼저 물어봐.
3. 감사 로그 테이블 하나를 추가해줘 (변경자, 변경일시, 대상 타입/ID, 이전값,
   이후값, 변경 사유, 승인자, 결과) — PI-DSP-AUD-002 기준.
4. Prisma schema.prisma 파일로 작성하고, 마이그레이션까지 실행해서 로컬
   SQLite DB를 생성해줘.
5. 각 엔티티에 대해 seed 스크립트를 만들어서, main-concept1.vercel.app
   스크린샷에 나온 홈 화면 하나를 이 모델로 표현한 샘플 데이터를 넣어줘.
   (예: Container "메인화면" → Template "로그인 기본" → Corner "혜택줍기",
   "VIP Pick", "추천" 등 → 각 Corner에 배너형/혜택형 Component 2~3개 →
   각 Component에 텍스트/이미지/CTA Atom들)

스키마를 다 만들고 나면, ERD를 텍스트(mermaid)로 그려서 나한테 보여줘.
내가 확인하기 전까지 다음 단계(UI) 작업은 시작하지 마.
```

---

## Phase 2 — Atom / Component / Corner 등록 UI (재료 만들기)

```
Phase 1의 스키마를 기준으로, 어드민 화면 중 "재료 등록" 영역을 만들어줘.
라우트는 /admin/atoms, /admin/components, /admin/corners 로 나눠줘.

1. Atom 등록 화면
   - 유형 선택(텍스트/버튼/이미지/아이콘/배지/가격/혜택문구/CTA/정보값)
   - 유형이 이미지/아이콘이면 대체텍스트 입력을 필수로 만들고, 비어있으면
     저장은 되지만 "검수 요청 가능" 상태로는 못 가게 해줘 (draft는 허용,
     review 전환은 차단 — PI-DSP-CMP-004 기준).
   - 목록 화면에서 유형별 필터링 가능하게.

2. Component 등록 화면
   - 유형 선택(정보형/행동형/혜택형/선택형/배너형/상품형)
   - Component를 만들 때 이미 등록된 Atom 중에서 골라서 조합하는 UI
     (필수 Atom / 선택 Atom 구분)
   - 이 Component가 어떤 Corner 유형에 쓰일 수 있는지(allowed_corner_types)
     체크박스로 지정

3. Corner 등록 화면
   - Corner 유형 선택(상품형/배너형/혜택오퍼형/업무진입형/상태안내형/
     콘텐츠안내형/개인화추천형/고정필수노출형)
   - Corner 유형을 고르면, 그 유형에 허용된 Component 유형만 이 Corner에
     추가할 수 있게 자동으로 제한해줘 (드롭다운/선택 목록에서 다른 유형은
     아예 안 보이거나 비활성화)
   - Corner 안에 이미 등록된 Component 인스턴스를 드래그앤드롭으로 순서
     조정할 수 있게 해줘 (dnd-kit 사용)
   - 최대 노출 개수를 넘는 Component를 추가하려고 하면 경고 표시

각 목록 화면은 상태(사용/미사용 or draft/review/approved 등 Phase 1에서
정한 값)를 뱃지로 보여주고, 필터링 가능하게 해줘.

UI 디자인은 shadcn/ui 기본 스타일로 충분해. 화려하게 만들지 말고 운영
업무용 어드민답게 표 중심, 정보 밀도 높게 만들어줘.
```

---

## Phase 3 — Template / Container 빌더 (블록 조립 화면)

여기가 "블록처럼 조립"의 핵심입니다. 레퍼런스 스크린샷을 이 프롬프트와 함께
클로드 코드에 보여주면서 진행하세요.

```
이제 진짜 "블록 조립" 빌더 화면을 만들어줘. /admin/templates/[id]/builder 라우트.

1. 화면 왼쪽: 이 Template에 아직 안 쓰인, Corner 유형과 상관없이 등록된
   Corner들의 목록 (검색/필터 가능)
2. 화면 가운데: 실제 화면 미리보기 영역. 디바이스 프레임을 선택할 수 있게
   해줘 (iPhone 15 Pro 393x852, Android 360x800 등 — reference 스크린샷의
   프레임 선택 UI를 참고). 이 프레임 안에 현재 Template에 배치된 Corner들이
   세로로 쌓인 순서대로 렌더링되고, 각 Corner 안의 Component들이 실제
   Atom 값(문구/이미지/가격 등)으로 렌더링돼야 해.
3. 왼쪽의 Corner를 가운데 미리보기 영역으로 드래그하면 Template에 추가되고,
   가운데에서 Corner 순서를 드래그로 재배치할 수 있어야 해.
4. 화면 오른쪽: 선택된 Template의 메타 정보 편집 패널
   - condition_group 선택 (비로그인/로그인/커스텀 조건 추가 가능)
   - is_default 여부
   - 기간형이면 시작일시/종료일시
   - 저장 시 상태는 "초안 작성중"으로 저장

5. Container 관리 화면(/admin/containers)도 만들어줘.
   - Container 생성/목록 (메인화면, 이벤트 화면 등)
   - 하나의 Container에 여러 Template을 연결하고, 그중 하나를 기본
     Template으로 지정
   - Container 상세 화면에서 "이 Container에 연결된 Template 목록"을
     조건 그룹별로 보여주고, 각 Template의 빌더로 바로 이동하는 링크 제공

6. 최종적으로 Container를 골랐을 때, 조건 그룹별 Template 미리보기를
   나란히 비교해서 볼 수 있는 화면도 하나 추가해줘 (예: "비로그인일 때"
   vs "로그인일 때" 화면을 나란히 놓고 비교).

reference 폴더의 스크린샷 구조(혜택줍기, VIP Pick, 카테고리 섹션들이
세로로 쌓인 형태)를 참고해서, 실제로 이런 화면이 조립 결과로 나올 수
있는지 seed 데이터로 검증해줘.
```

---

## Phase 4 — 검수 · 승인 · 게시 워크플로우

```
정책서의 상태 전이표를 기준으로 검수/승인/게시 플로우를 만들어줘.
(정책서 "마. 상태 전이표" 섹션, PG-DSP-WFL-001, PG-DSP-RBK-001 참고)

1. Template 상세 화면에 "검수 요청" 버튼 추가.
   - 필수값(제목, 노출 조건, 랜딩값, 대체텍스트 등)이 다 채워졌는지
     체크하고, 하나라도 비어있으면 검수 요청을 막고 어떤 값이 빠졌는지
     알려줘. (PI-DSP-WFL-001)
2. "검수 대기" 상태의 Template에 대해 승인/반려 처리 화면.
   - 반려 시 반려 사유 입력 필수, 상태는 "수정 필요"로.
   - 승인 시 상태는 "승인 완료"로.
3. "승인 완료" 상태에서 게시 예약(시작/종료 시각) 설정 → "예약 대기" →
   (시각 도래 시뮬레이션 버튼으로) "게시 중"으로 전환.
   같은 조건 그룹, 같은 Container 안에서 게시 기간이 겹치는 Template이
   있으면 경고를 띄워줘 (동일 조건 그룹 내 중복 게시 금지 — 정책서
   "3. 조건 판정" 항목 참고).
4. "게시 중" 상태에서 "긴급 중지" 버튼 → 상태 "게시 중지"로 전환하고
   중지 사유, 실행자, 실행 시각을 필수 기록. 이후 "롤백" 버튼으로 직전
   승인 완료 버전으로 되돌릴 수 있게 해줘. (Draft/Review/Rejected
   상태로는 롤백 불가 — PI-DSP-RBK-002)
5. 모든 상태 변경은 감사 로그 테이블에 기록되고, /admin/audit-log
   화면에서 대상별/기간별로 조회할 수 있게 해줘.

이 단계까지 끝나면 "재료 등록 → 블록 조립 → 검수 → 승인 → 게시"가
로컬에서 end-to-end로 한 번 돌아가는 상태가 되는 게 목표야. 다 끝나면
전체 플로우를 처음부터 끝까지 한 번 시연하듯 실행해서 나한테 결과를
보여줘 (스크린샷 or 화면 흐름 설명).
```

---

## Phase 5 — 확장 (MVP 이후, 필요할 때만)

정책서에는 있지만 MVP 이후로 미뤄도 되는 영역들입니다. 필요할 때 별도로 요청하세요.

- **배너 캠페인 통합 관리**: 하나의 캠페인이 여러 Container/Corner에 동시에
  걸리는 구조 (PG-DSP-BNR-001)
- **AI 운영 보조**: 문구/이미지 조합 후보 제안, 운영자 확정 전엔 게시 불가 (PG-DSP-AI-001)
- **A/B 실험·가드레일**: 실험군/대조군 분리, 중지 조건, 결과 판정 (PG-DSP-ABT-001)
- **성과·피로도 리포트**: 노출수/클릭/CTR/숨김/이탈 지표 (PG-DSP-MET-001)
- **표기 검증 자동화**: 가격·혜택 표기, 핵심 메시지 1개 제한 등 (PG-DSP-ACC-001)

---

## 진행하면서 참고할 팁

- 각 Phase 끝에서 클로드 코드가 "이해한 내용을 다시 설명"하게 시키면, 정책서를
  잘못 해석했을 때 코드를 짜기 전에 걸러낼 수 있어요.
- Phase 3(빌더 UI)이 가장 손이 많이 갈 거예요. 레퍼런스 스크린샷을 그때그때
  같이 보여주면서 "이 섹션이 실제로 이렇게 조립돼야 해" 라고 구체적으로
  짚어주는 게 훨씬 정확한 결과를 만들어요.
- 정책서에 있는 정책 ID(`PI-DSP-xxx`)를 프롬프트에 인용해두면, 클로드 코드가
  임의로 규칙을 지어내지 않고 문서를 근거로 판단하게 됩니다. 애매한 부분이
  나오면 "정책서 PI-DSP-xxx 다시 확인해줘"라고 지시하세요.
