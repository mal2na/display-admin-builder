# CLAUDE.md — 전시/관리 어드민 빌더

> 이 파일은 프로젝트의 **불변 컨텍스트**다. 새 작업을 시작할 때 항상 먼저 읽고, 여기 정의된
> 개념·상태값·정책 기준을 **임의로 바꾸지 말 것**. 애매하면 정책서에서 정책 ID를 찾아 확인하고,
> 그래도 애매하면 코드를 쓰기 전에 사용자에게 먼저 물어본다.

---

## 1. 프로젝트 목적

개발자 없이도 **운영자가 "블록 조립"하듯 화면을 구성**할 수 있는 전시/관리 어드민 빌더를 만든다.

- `Atom → Component → Corner → Template → Container` **5단 계층 구조**로 화면을 표준화한다.
- 운영자는 화면을 새로 코딩하지 않고 **표준 구성 단위를 조합**해 메인화면·이벤트화면 등을 만든다.
- 그렇게 만든 화면을 **검수 → 승인 → 게시**까지 어드민 안에서 처리할 수 있어야 한다.
- 대상 운영자(액터 `ACT-DSP-001`)는 전시 구성, 검수·승인, 게시, 채널 노출 상태, 성과 개선을 담당한다.

---

## 2. 핵심 개념 — 5단 계층 (이 정의를 그대로 따를 것)

계층은 **작은 단위 → 큰 단위** 순서다. 아래로 갈수록 감싸는 관계다.

```
Atom  ──재료──▶  Component  ──올림──▶  Corner  ──배치──▶  Template  ──매핑──▶  Container
(표시요소)        (기능모듈)          (화면영역)         (레이아웃)          (실제 화면/영역)
```

### 2.1 Atom (가장 작은 표시 요소)
- 문구, 이미지, CTA 버튼, 가격, 아이콘, 배지, 대체텍스트처럼 화면을 이루는 **가장 작은 표시 요소**.
- **단독으로 화면을 구성하지 않는다.** 오직 Component의 재료로만 쓰인다.
- 정책서 기준 대표 유형: 텍스트 Atom / 버튼 Atom / 이미지 Atom / 정보 Atom
  (사용 가능 유형·속성은 디자인 라이브러리 기준 `ACT-DSP-005`를 따른다).

### 2.2 Component (Atom을 조합한 기능 모듈)
- Atom을 조합한 기능 모듈. 배너, 상품 카드, 탭 메뉴, 리스트, 퀵메뉴, 요약 정보 등.
- **반드시 아래 6개 유형(type) 중 하나를 가진다:**
  `정보형 / 행동형 / 혜택형 / 선택형 / 배너형 / 상품형`
- 노출 조건에 따라 구성 데이터가 달라질 수 있다.

### 2.3 Corner (Component를 올려놓는 화면 영역)
- 하나 이상의 Component를 묶어 **화면의 특정 영역**을 구성하는 단위 (예: "이용 요약", "추천 혜택", "메인 배너").
- **Corner 유형(8종)에 따라 붙일 수 있는 Component 유형이 제한된다.** (핵심 규칙 — 아래 매핑표)
- 구성 기준: **정렬(노출 순서), 최대 노출 개수**, 타이틀, 노출 조건, 우선순위.

**Corner 유형 → 허용 Component 유형 매핑 (정책서 기준 baseline):**

| Corner 유형 | 허용 Component 유형 |
|---|---|
| 상품형 | 상품형 |
| 배너형 | 배너형 |
| 혜택·오퍼형 | 혜택형, 정보형, 행동형, 배너형 |
| 업무 진입형 | 행동형, 정보형, 선택형 |
| 상태 안내형 | 정보형, 행동형 |
| 콘텐츠 안내형 | 정보형, 행동형, 배너형 |
| 개인화 추천형 | 정보형, 혜택형, 선택형, 행동형, 배너형 |
| 고정·필수 노출형 | 정보형, 행동형 |

> 이 매핑은 정책서에 **예시(baseline)**로 명시돼 있다. 별도 매핑 테이블/상수로 관리하고,
> Corner 유형을 고르면 허용되지 않은 Component 유형은 UI에서 아예 못 붙이게 제한한다.

### 2.4 Template (Corner를 순서/배치로 구성한 레이아웃)
- 여러 Corner를 **순서/배치**로 구성한 화면 레이아웃(화면의 골격).
- **분기**: 로그인 여부, 고객 세그먼트 등 **조건 그룹**에 따라, 같은 Container라도 다른 Template으로 분기될 수 있다.
  (분기 기준 예: 로그인/비로그인, 고객 유형, 보유 상품, 앱/웹, OS)
- **기간형 Template**: 시작·종료 기간을 가져 특정 기간만 다른 화면을 보여줄 수 있다.

### 2.5 Container (채널에 실제로 나가는 화면/영역 단위)
- 메인화면, 메뉴, 기획전, 혜택 영역처럼 **채널에 실제로 반영되는 화면/영역 단위**.
- **관계: 화면과 1:1, Template과 1:N.**
- **삭제하지 않는다.** 채널이 호출하는 화면 ID이므로 **상태값으로 관리**한다(active / inactive 등 soft-delete).
- **기본 Template을 반드시 1개 가진다** (`default_template_id`).
- 실제 반영 기준: `Container + Template + 고객 상태 + 노출 조건`.

---

## 3. 전시 상태 흐름 (10단계 — 이 상태값을 그대로 사용할 것)

정책서 상태 코드 `ST-DSP-001 ~ ST-DSP-010`와 1:1 일치한다.

**대표(정상) 흐름:**
```
초안 작성중 → 검수 대기 → (반려 시) 수정 필요 → 검수 대기 → 승인 완료 → 예약 대기 → 게시 중 → 종료
```

**예외(복구/제한) 흐름:**
```
게시 중 → 게시 중지 → 롤백 완료
게시 중 → 개인화 제한 → 게시 중 (복귀)
```

| 코드 | 상태명 | 정의 |
|---|---|---|
| ST-DSP-001 | 초안 작성중 | 구성/소재 등록 중, 아직 검수 요청 전. (AI 후보 생성은 이 상태의 보조 활동) |
| ST-DSP-002 | 검수 대기 | 등록 완료 후 검수·승인 대기. |
| ST-DSP-003 | 수정 필요 | 필수정보 누락·링크 오류·표기 미흡·랜딩 불일치로 반려된 상태. |
| ST-DSP-004 | 승인 완료 | 검수 통과, 예약 또는 즉시 게시 가능. |
| ST-DSP-005 | 예약 대기 | 게시 예약 완료, 시작 시각 미도래. |
| ST-DSP-006 | 게시 중 | 고객에게 노출 중, 성과·이상 징후 수집. |
| ST-DSP-007 | 게시 중지 | 오노출·링크오류·심의·성과이상으로 노출 중지. |
| ST-DSP-008 | 종료 | 노출 기간 종료, 더 이상 미표시. |
| ST-DSP-009 | 롤백 완료 | 이전 승인 버전/기본 전시로 되돌린 상태. |
| ST-DSP-010 | 개인화 제한 | 숨김·관심없음·빈도초과 등으로 특정 전시가 제한된 상태. |

**전이 규칙 핵심 (정책서 "마. 상태 전이표"):**
- `초안 작성중 → 검수 대기`: 제목·노출 기간·랜딩·대상 조건·**대체텍스트** 등 필수 항목이 채워져야 전환.
- `검수 대기 → 수정 필요`: 필수 누락 / 대체텍스트 없음 / 잘못된 링크 / 랜딩 불일치 중 하나라도 있으면.
- `수정 필요 → 검수 대기`: **반려 사유별 보완 내용 + 변경 사유가 기록**되어야 재검수.
- `승인 완료 → 예약 대기`: 시작·종료 시각, 슬롯, 대상 고객군 확정.
- `예약 대기 → 게시 중`: 예약 시각 도래 + 링크·**노출 충돌**·대상 조건 판정이 모두 유효할 때.
- `게시 중 → 게시 중지`: 긴급 중지. **중지 사유·실행자·실행 시각 필수 기록.**
- `게시 중지 → 롤백 완료`: 복원 대상 버전과 승인자 확인 시. (Draft/Review/Rejected로는 롤백 불가)
- `개인화 제한 → 게시 중`: 제한 기간 만료 또는 해제 조건 충족 시 후보로 복귀.

---

## 4. 참고 화면 / 디자인 기준

프로젝트 폴더의 이미지 3장은 각각 역할이 다르다. **혼동하지 말 것.**

| 파일 | 역할 | 어떻게 쓰나 |
|---|---|---|
| `티우주.png` | **현재 빌딩 구조**를 만드는 이미지 (T우주 관리자포털 "전시화면 관리" 실제 화면 — 컨테이너 등록, 코너 유형 관리, 컴포넌트 노출유형 관리 등) | 빌더가 최종적으로 **만들어낼 수 있어야 하는 화면의 복잡도/구조 기준**. 빌더 UI가 이걸 똑같이 따를 필요는 없음. |
| `레퍼런스.png` | 빌더 UI로 **실제로 보고 싶은 형태** (DS-portal 슬롯 빌더: 좌측 슬롯 트리 + 중앙 디바이스 프레임 미리보기 구조) | 빌더 자체의 화면 구성/인터랙션 레퍼런스. "저렇게 실제로 볼 수 있는 UI"를 지향. |
| `bss ui.png` | **컬러/색감** 기준 | 빌더의 컬러·색감은 **이 BSS UI를 따른다**. |

---

## 5. 정책서 사용 규칙

- 원본: `NC_전시관리기능_정책서_간소화_v0.31.html` (POL-DSP v0.31, 같은 폴더).
- 데이터 모델이나 정책 판정 기준이 **애매하면 반드시 이 파일에서 정책 ID를 찾아 확인**하고 반영한다.
- **마음대로 새 규칙을 만들지 말 것.** 근거가 되는 정책 ID를 함께 밝힌다.

**자주 참조할 정책 ID:**
- `PI-DSP-CMP-003` — Corner 유형 선택 시 연결 가능한 Component 유형 자동 제한.
- `PI-DSP-CMP-004` — 이미지/아이콘 Atom 대체텍스트 필수 (없으면 draft는 허용, review 전환 차단).
- `PG-DSP-WFL-001` / `PI-DSP-WFL-001` — 검수 요청 필수값 체크 및 워크플로우.
- `PG-DSP-RBK-001` / `PI-DSP-RBK-002` — 롤백 (Draft/Review/Rejected 상태에서 롤백 불가).
- `PI-DSP-AUD-002` — 감사 로그(변경자/일시/대상/이전값/이후값/사유/승인자/결과).
- 확장(Phase 5): `PG-DSP-BNR-001`(배너 캠페인) / `PG-DSP-AI-001`(AI 보조) / `PG-DSP-ABT-001`(A/B) /
  `PG-DSP-MET-001`(성과) / `PG-DSP-ACC-001`(표기 검증).

---

## 6. 기술 스택 (제안 — 바꾸려면 이유를 먼저 말할 것)

- **Next.js (App Router) + TypeScript**
- **저장소:** 처음엔 로컬 SQLite (better-sqlite3 또는 Prisma + SQLite). 나중에 실제 DB로 교체 가능하도록
  **스키마를 분리해서 설계**한다.
- **드래그앤드롭:** dnd-kit (빌더 UI의 Corner/Component 재배치).
- **UI:** Tailwind + shadcn/ui. 컬러/색감은 `bss ui.png`(BSS UI) 기준.

> 스택을 조정할 필요가 생기면, 코드를 쓰기 전에 **왜 바꾸는지 이유를 먼저 설명**하고 합의한다.

---

## 7. 작업 원칙

- 각 단계 끝에서 **"이해한 내용을 다시 설명"**하고, 사용자가 확인하기 전에는 다음 단계로 넘어가지 않는다.
- 개념·상태값은 이 문서를 **단일 진실 원본(source of truth)**으로 삼는다. 문서와 코드가 어긋나면 문서 기준으로 맞춘다.
- 규칙 판단이 애매하면: ① 정책서 정책 ID 확인 → ② 그래도 애매하면 사용자에게 질문. **임의 추정 금지.**
